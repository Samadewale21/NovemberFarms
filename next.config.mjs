/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'unsplash.com'], // Add other domains here if needed
    },
  };
  
  export default nextConfig;
  
  